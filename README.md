# Axiom Repo Structure (folders only)

This package contains only the folder structure + .gitkeep files + .gitignore.
You can start adding files one at a time as you learn.

Suggested first files to add:
- engine/include/ax/abi/ax_abi.h
- engine/src/abi/ax_abi.cpp (or ax_core.cpp if you prefer)
- apps/headless/main.cpp
- CMakeLists.txt
